# G-016 saved episode summary — environment-blocked checkpoint

**Overall G-016 is implemented but still awaits executable acceptance.** The existing gated iOS calendar has a separate View summary action that reads one canonical saved episode, displays its complete ordered medicines and supported exact doses, recorded timing/relief/signs/context, full notes and missing values, and isolates account/episode/request generations. The existing editor and pending-save path remain in source. Actual runtime behavior still requires the checks below.

The requested strategy correction remains in PROJECT_STATE.md, PMF_STATE.md, DECISION_LOG.md and RECOMMENDED_NEXT_ACTIONS.md: scalable acquisition/engagement while improving the product; substantial free/ad-free value; finances as context; viable evidence-scaled research/medical capability; contextual disclosure rather than mechanical generic disclaimers. G-016 does not change that direction or the D028 core-before-imports commitment.


## Latest diagnostic preparation — D04-E1 awaiting independent review

The [distinct D04-E1 wrapper repair](environment-d04-entrypoint-repair-e1/HANDOFF.md) pins D02 launch/input manifests and validates the exact Probe argv/cwd before dispatch. The75 focused fake/temp-file tests passed once, including refusal of the independent reviewer’s substituted executable/project. All129 preserved evidence files and28 scoped repository files still match. Candidate2 core/adapter acceptance is retained unchanged; entrypoint1 is preserved with a repair-required review verdict. The guard is explicitly point-in-time: later adapter file reads are not locked or immutable. The operator command has not been issued or run, Terminal remains denied, and G016 executable acceptance remains blocked.

**Latest source checkpoint:** [G016-R01/R02 repair packet](source-repairs-r01-r02/HANDOFF.md) supersedes the initially reviewed source. The summary now accepts ordinary stored-revision0/lifecycle1 episodes, and the generic Updated claim has been removed. The legacy-note fixture retains structured revision/lifecycle instead of inventing advancement. Both findings are independently accepted at the source/contract level; executable acceptance remains blocked.

## What changed

- Added a saved-data projection, account/generation-aware reader and read-only summary screen. Refresh removes the prior snapshot while loading; malformed/wrong/deleted/unavailable results are not treated as complete saved summaries.
- Added one distinct calendar action and separate summary sheet, preserving selected day/month and the existing editor sheet/save guards. Empty medicines say “No medicine entries recorded”; full saved notes remain available. Context is recorded context, not a proven cause. No internal account/import/file IDs appear in the summary projection.
- Added Debug-only synthetic complete/empty/malformed/deleted/wrong/retry/account/edit/large-text scenarios, request capture and focused model/store/UI tests. The new tests are written and typechecked, but **not executed**.
- Moved G-015's three existing date bullets verbatim into Gauge Scorer; reconciled G-015's independent acceptance in current recovery docs. No scoring behavior or tests were changed/rerun by G-016.

[All14 changed files](SOURCE_FILES.md), [G016-only before-image diff](G016-only.diff), [28-file scoped source manifest](source-manifest.json) and preserved current sources are included. Thirteen preparation hashes and all29 accepted G015 hashes were checked at startup. Two outside-repo preparation sources, accepted G014/G015 packets and prior dirty work are retained. HEAD remained `1e0b69ef3b841d3a5ba050c56dea4fc3a4474e29`; no commit/stage/reset occurred. Other watcher workflow/open-question/test changes are outside this scope and were preserved.

## Verification actually completed

| Check | Result |
|---|---|
| Direct Swift production-source typecheck | **PASS:99 current app sources**,53.35s, including all new summary source; prior accepted SDK/target/import flags and dependencies reused. |
| Direct testable module emission | **PASS**,62.89s; isolated evidence-folder output, accepted build products retained. This is not a linked app bundle. |
| Model/store plus existing gate test-source typecheck | **PASS on repaired source** using the previously corrected simulator framework paths. |
| New UI test-source typecheck | **PASS on repaired source**. No UI test execution or visual acceptance. |
| Final syntax and whitespace | **PASS:nine affected Swift sources**; git diff --check passed. |
| G015 documentation | **PASS:all three bullets unchanged, exactly once under Gauge Scorer**. |
| Xcode model/test attempts | **Three compiler-discovery stalls; zero executed tests**. All stopped with exit75, with logs/partial result bundles retained. |
| Actual G016 model/UI checks, screenshots, request captures, fresh Release build/exclusions | **NOT RUN / NOT VERIFIED** because Xcode planning did not reach source compilation. |

Fresh repair checks are in [repair commands/results](source-repairs-r01-r02/commands-and-results.json) and [current typechecked hashes](source-repairs-r01-r02/typechecked-source-hashes.json). Parent direct compiler files and timings in historical result JSON describe the pre-repair checkpoint. See [commands and results](commands-and-results.json), [verification summary](verification-summary.json), [typechecked source hashes](typechecked-source-hashes.json), [direct compiler command](direct-typecheck-command.json) and the individual typecheck result/log files. The first standalone test-source command lacked Testing/XCTest search paths; its failures are preserved alongside the passing corrected command. No product patch was needed for those command failures.

## Concrete blocker and recovery already attempted

**G016-B01 — local Xcode build planning stalls at CreateBuildDescription / clang discovery.** The direct identical clang probe passes in0.0325s, but Xcode's probe was blocked writing to its build service. The first owned build was stopped after over ten minutes with unchanged output. A fresh owned process and one later process after narrowly authorized indexing-service recovery each stopped automatically at150 seconds without output. The later recovery retained Xcode and its documents and did not clear caches/DerivedData; its service and probe children exited naturally. No further Xcode retry loop remains.

The cause is unresolved. An external concurrent-service report was a diagnostic lead, not proof; the final retry still stalled with only this task's build service visible. [Exact diagnosis, samples and recovery limits](BUILD_BLOCKER.md) and [service recovery record](xcode-service-recovery.json) are included. Broader app/environment recovery requires a new evidence-based decision from the coordinator; no new Jennifer input is identified by this source checkpoint itself.

## G016-D02 isolated environment diagnosis

The [single dependency-free C project probe](environment-d02/DIAGNOSIS.md) reproduced the same compiler-discovery stall with fresh task-owned build data. It ended after66.118s including cleanup, exit75; the compiler sample again stayed in `write`. All28 scoped repository hashes remained unchanged, Xcode stayed open and no owned process remained. Gaia source, packages and reused build data are not required for the failure. The precise shared launch-environment versus installed planner/toolchain cause remains unresolved. No working recovery was proved, so no further Gaia attempt was made. The coordinator's next narrow investigation can compare an alternate-parent launch of this exact preserved project; it has not been executed. R01/R02 source acceptance is retained, and actual model/UI/Release checks remain pending.

## G016-D03 controlled launch-limit comparison

The [single child-NOFILE256 probe](environment-d03/DIAGNOSIS.md) reused the D02 C project and launch settings with fresh outputs. The child soft limit was observed at256 immediately before exec, its hard limit was unchanged, and the runner was immediately restored to1048575. The same compiler-write stall persisted. Controlled interruption ended after66.301s, exit75; no source compile/archive or Gaia test ran. All28 scoped repository hashes and accepted source repairs remain unchanged, and no owned process remains. Lowering the inherited limit did not provide a candidate recovery. The experiment is stopped; a next diagnostic needs a new discriminating factor selected from this evidence. The earlier alternate-parent proposal remains unexecuted and is not established as a non-disruptive available path.

## G016-D04 native Terminal comparison — not launched

The [prepared alternate app-parent experiment](environment-d04/DIAGNOSIS.md) could not launch: Computer Use explicitly denied `com.apple.Terminal` access for safety reasons. No UI state, new Terminal session or build was created. The one-shot runner is preserved and syntax-parsed, with native Terminal ancestry required before any build; it has not executed. All28 repository hashes and retained D02/D03 evidence remain unchanged. D04 adds zero synthetic/Gaia attempts and no build-cause finding. The required authorized native app-control surface is unavailable; the conditional Gaia verification sequence was not reached. No bypass or owner question was introduced.

## D04-H1–H4 prepared-runner hardening — review only

Independent inspection found deadline, lifecycle, inventory and success-status defects in the original unexecuted D04 runner. A [distinct frozen candidate](environment-d04-runner-repairs/HANDOFF.md) now has71 passing synthetic checks, including mocked native timer/launch/signal/file adapters. All28 repository hashes and original D02/D03/D04 evidence remain unchanged. This repair is ready for independent source/synthetic review only; it does not establish native timing, a build recovery or permission to run. The original runner remains historical and must not be handed off. Native Terminal remains explicitly denied, D04 still has zero builds, and overall executable G-016 acceptance remains pending.

## Candidate2 — late-cancellation finalization correction

Independent review retained candidate1's deadline, inventory, ownership/cleanup and compile/archive closures, but reproduced one stale-success result when SIGTERM arrived during terminal-checkpoint writing. The [distinct candidate2 packet](environment-d04-finalization-repair/HANDOFF.md) reconciles received cancellation after the handler-restoration attempt and before final publication. All28 focused tests and one99-case full suite pass; the native adapter and deadline values are unchanged. Candidate1 and all original evidence remain preserved. Candidate2 was subsequently independently accepted at source/synthetic scope; its CLI still refuses native execution. Separate wrapper preparation and D04-E1 review status are recorded above; D04/Gaia build counts remain unchanged.

## D04 manual entrypoint1 — historical preparation; D04-E1 repair required

Candidate2/F1 is independently accepted for source/synthetic scope. A [small fixed-project wrapper and exact operator packet](environment-d04-manual-entrypoint/HANDOFF.md) now pair its unchanged core/adapter with explicit-flag/isolation checks, pinned source bytes, one invocation, exit mapping and a no-overwrite receipt. All42 focused wrapper tests pass using fakes/temporary synthetic modules. The accepted supervisor suites were not repeated, all28 repository hashes and prior evidence remain unchanged, and the future real output/receipt paths remain absent. The packet is ready for independent review before the coordinator gives any owner action. Native Terminal denial and G-016 executable-verification blockers remain in force; no native/Xcode/Gaia run occurred.

## Resume and reviewer actions

1. Verify the source manifest against current files, inspect the G016-only diff and retain all failed evidence. Review the pure projection/store and actual calendar integration for source-level findings; do not mark runtime acceptance from a static review.
2. Restore working local Xcode build planning through a separately evidenced recovery, then follow [the prepared sequential verification commands](NEXT_VERIFICATION.md). Intended execution is45 new model/store cases plus one existing gate; nine new actual UI journeys plus one existing pending-editor compatibility case. Confirm actual executed counts from result bundles.
3. Inspect normal/large-text screenshots and full notes; validate selected-ID GET-only captures and exactly one explicit edit POST/new canonical note with unchanged structured revision/lifecycle on the edit journey. Rebuild unsigned arm64 Release and check all15 fixture markers. No existing Release binary can satisfy the current source check.
4. Update source hashes/docs/report only after these pass, publish review_ready and obtain independent coordinator acceptance. [Exact remaining physical/signed-release actions](DEVICE_RELEASE_ACCEPTANCE.md) are prepared separately; local acceptance does not imply they were performed.

## Boundaries and limits

Calendar and summary entry remain Debug-only/default-off, with Release fixture code excluded by source guards pending fresh binary verification. No backend/schema/dependency changes, deployment, production SQL, real-user data, imports/personal exports, PDF/CSV/share/export format, account/store action or outreach occurred. Android/member-hub parity is intentionally deferred. Pending requests remain the existing in-memory editor workflow; no durable queue or new medicine identities were introduced. G-016 does not complete the full requested migraine update or authorize its release. Only the coordinator writes portfolio registers.

Entrypoint1’s independent review subsequently reproduced launch-data redirection despite42 passing focused cases. It is retained unchanged as evidence; the latest distinct D04-E1 packet and pending repair review supersede its earlier review-ready status.
