# Qualified local-primary EarthScope consumption

G053, September 24, 2026. Implemented locally, default off; not a production acceptance claim. The [draft-only worker wire](EARTHSCOPE_DRAFT_WORKER.md) and all its false eligibility flags remain unchanged. Activation requires the separate owner-selected D044 qualification record and exact installed configuration.

`EARTHSCOPE_WRITER_MODE` defaults to `legacy`. Only explicit `local_primary` selects `scripts/earthscope_local_primary.py`; other values fail. That path requires `EARTHSCOPE_LOCAL_ACCEPTANCE_ID`, `EARTHSCOPE_LOCAL_VALIDATOR_ID`, `EARTHSCOPE_LOCAL_CAPTION_PROFILE`, dedicated `EARTHSCOPE_DRAFT_PREPARER_DSN` and the matching `EARTHSCOPE_DRAFT_WORKER_ID`. Expected validator/profile must match the accepted Local AI version; their names are not evidence of acceptance. The existing cloud Supabase service key performs public-row publication separately; the preparer and Mac never receive that authority.

The selected default-off profile is `structured_authored_caption_v2_policy_assembly_v1` with validator `gaia-two-stage-policy-assembly-v1+compact-machine-v1`. Root accepted the L117 typed temporal source correction, not the retained September 24 copy: its day-minimum magnetic direction and event-count wording remain rejected. The two native stages own caption/title/snapshot; policy assembly owns the disclosed boundary/actions/hashtags/selections. Source-policy hashes and a fresh editorial acceptance record are required in addition to those identifiers.

The dedicated preparer connects through the project session pooler on port 5432 as `gaia_earthscope_preparer_runtime`; it may explicitly SET the preparer role and cannot inherit it or set the backend role. `scripts/earthscope_preparer_identity.py` verifies this identity and effective permissions read-only before a primary daily run. Both this proof and the consumer require certificate-verified TLS using `EARTHSCOPE_DRAFT_DB_CA_PEM` materialized as `PGSSLROOTCERT`. The import-only application settings receive the same dedicated DSN as `DATABASE_URL`; no general API database credential is added to GitHub.

`.github/workflows/earthscope_writer_review.yml` has no schedule and defaults to identity-only verification. Its separately selected `review` mode requires an authorization reference, current UTC/local day, and invokes `--review-only` with no publisher/service/model credentials. A reference does not grant a native allowance: the Mac's exact job authorization remains independently enforced. This workflow does not change the production writer mode or render/publish a draft.

The preparer serializes initial day selection, reuses the existing version's frozen facts/deadline and waits up to 600 seconds in the daily workflow. It does not silently advance a failed version, regenerate after an uncertain acknowledgement, extend a lease or fall back to yesterday, templates or OpenAI. Current UTC and configured local day must agree. Complete returned bundle, classification, claim/job/facts/outcome/candidate/bundle hashes and validator/profile bindings are checked before consumption.

The bundle maps verbatim to title/caption/hashtags, named sections, all three social variants and five reel beats. The compatible web JSON retains facts-as-of rather than inventing a generation-time observation. Null signals remain unavailable. Solar wind uses the neutral observed label because the shared wire can represent a latest value or a mean; raw provenance remains available. A conflicting same-day public row stops the run; insert-ignore plus exact readback reconciles an identical prior commit without overwriting it.

The daily workflow carries target date and canonical post hash to cards, reels and Meta via the same artifact. Primary cards avoid legacy latest lookups, rule-based quick tips, section truncation helpers and the legacy current-metrics mirror. Primary social captions preserve authored paragraphs/CTAs rather than adding a generic CTA. Reels retain music-only operation. Run-specific media revisions and downloaded exact images bind the published video and captions to their rendered inputs; latest pointers remain for existing website consumers.

The additive migration `20260924145345_earthscope_complete_public_copy_history.sql` appends bounded complete-copy excerpts to the existing public-only history view and creates an RLS-protected `content.earthscope_delivery_attempts` ledger. Existing service_role can SELECT/INSERT and UPDATE only state/post_id; public/anon/authenticated and queue roles cannot use it. No mixed member table grant, credential, LOGIN or role membership is created. Apply after unchanged G043 and revised G046, using the reviewed service-login grant recipe.

Before each primary Meta carousel/reel attempt, the cloud reserves a unique day/channel intent bound to post/destination hashes and media revision. A confirmed exact prior publication is skipped. A conflict, unfinished intent or uncertain response requires platform reconciliation before any further action. Primary Meta operations have one create/publish attempt and no alternate-command fallback. This is at-most-one automatic attempt, not an exactly-once guarantee by the external platform. Never clear an uncertain intent merely to retry a scheduled job.

`--review-only` exercises the real queue and writes local review artifacts without touching daily_posts or publishing. It uses a fixed `review-only-not-accepted` marker. Only the card renderer's explicit `--dry-run` permits that marker; ordinary card rendering, reel and Meta consumption reject it. Use a disposable media directory for review output. `--review-only` does not authorize another model call or bypass the Local AI worker's generation controls.

Scheduling remains the existing `5,35 11-15 * * *` UTC cron with a 09:05 America/Chicago earliest-time gate. Eligible nominal slots: 14:05–15:35 UTC in CDT (09:05–10:35 local), 15:05/15:35 UTC in CST (09:05/09:35 local). GitHub runner delay is variable. The Mac should already poll before cloud enqueue; a 10:35 local-only start misses earlier jobs, and 10:35 UTC ingestion is unrelated. The proposed disabled Mac polling window is 08:55–12:15 America/Chicago, every 60 seconds while idle, one process/claim at a time, subject to root/Local AI selection. The 600-second queue deadline starts at cloud enqueue. Out-of-window/offline/busy/failed delivery stops explicitly; no old content is relabeled current.

Rollback starts by restoring the writer mode and disabling selected queue/worker/shadow activation as appropriate. Preserve queue/publication evidence; no schema drop is needed. A day partly published must be reconciled before a legacy rerun. Both API and existing ingest worker deploy On Commit from main, so source rollout/reversal affects both. Default-off source installation is distinct from hosted schema/identity/backup preparation and final activation.

Current setup evidence and exact steps: [G053 final installation bundle](/Users/gennwu/Documents/Codex/2026-09-05/we/outputs/g053-final-installation-bundle/INSTALL.md). Tests: `tests/api/test_earthscope_local_primary.py` uses synthetic data, fake HTTP and the owned Unix-socket PostgreSQL fixture; it never accepts a live database DSN. No actual delivered-copy, medical-validation, growth or financial conclusion follows from those tests.
